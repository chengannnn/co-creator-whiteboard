# Ralph Progress Log
Started: Sun Apr 12 16:08:46     2026
---

## Codebase Patterns
- Monorepo uses npm workspaces with packages/frontend and packages/backend
- Backend uses ESM modules (`"type": "module"`) with tsx for development
- Frontend uses Vite + React 19 + TypeScript strict mode
- ESLint 9 flat config (eslint.config.js) required for both packages
- Backend ESLint needs `typescript-eslint` (not just `globals.node`) to parse TypeScript generics like `Map<string, Set<WebSocket>>`
- Dev servers: frontend on port 5173, backend on port 3001
- Vite proxies `/api` and `/ws` to backend during development
- Pure utility functions used by `useCallback` may need `/* eslint-disable react-hooks/exhaustive-deps */` if they don't depend on component state — ESLint auto-fix wraps them in `useCallback` which can cause parse errors
- Room state is managed in-memory on backend using `Map<string, Set<WebSocket>>` with cleanup timers
- WebSocket connection established on room mount; roomId sent via `join_room` message
- Use `forwardRef` + `useImperativeHandle` to expose child component methods (e.g., canvas export) to parent components
- rough.js uses `strokeLineDash` (e.g., `[8, 6]`) for dashed strokes — NOT `strokeStyle`
- Move pure helper functions to module level for stable references that don't trigger re-renders in hook dependency arrays

---

## [2026-04-12] - story-1
- Implemented project scaffolding with monorepo structure
- Files changed:
  - Root: package.json (workspace config), .gitignore
  - packages/backend: Express + ws server, tsconfig.json, ESLint config
  - packages/frontend: Vite + React + TypeScript setup, react-router-dom
- Typecheck and lint pass for both packages
- **Learnings for future iterations:**
  - ESLint flat config requires explicit `globals.node` for backend TypeScript files
  - Running ESLint from root needs `--config` flag pointing to package's config file
  - npm workspaces resolve correctly with `npm run dev -w packages/<name>` pattern

---

## [2026-04-12] - story-2
- Implemented canvas with basic drawing tools (Select, Rectangle, Ellipse, Freehand)
- Files changed:
  - packages/frontend/package.json: added roughjs dependency
  - packages/frontend/src/types/shapes.ts: Shape type definitions (RectangleShape, EllipseShape, FreehandShape)
  - packages/frontend/src/components/Toolbar.tsx: Left toolbar with tool buttons and active state highlighting
  - packages/frontend/src/components/CanvasComponent.tsx: Full-screen canvas with rough.js rendering, mouse event handling for drawing
  - packages/frontend/src/App.tsx: Integrated Toolbar and CanvasComponent into WhiteboardRoom route
- TypeScript strict mode and ESLint pass
- **Learnings for future iterations:**
  - roughjs has built-in TypeScript types (`roughjs/bin/canvas.js` exports `RoughCanvas`)
  - `tsconfig.json` has `noUnusedLocals: true` and `noUnusedParameters: true` — clean imports carefully
  - Canvas needs to be resized on window resize and roughCanvas recreated after each resize
  - Freehand shapes store raw point arrays; rectangles/ellipses store bounding boxes
---

## [2026-04-12] - story-3
- Implemented shape selection and manipulation
- Features: click to select with dashed bounding box + 8 resize handles, drag to move, resize via handles, Delete/Backspace to remove, Escape to deselect, Ctrl+Z undo
- Files changed:
  - packages/frontend/src/App.tsx: added history state for undo support
  - packages/frontend/src/components/CanvasComponent.tsx: added selection, move, resize, delete, and undo logic with interaction mode state machine
- TypeScript strict mode and ESLint pass
- **Learnings for future iterations:**
  - ESLint auto-fix wraps pure functions in `useCallback` with `[]` deps — this can cause parse errors; use file-level `eslint-disable` comment instead
  - History-based undo stores full snapshots of shapes array (simple but memory-heavy for large canvases)
  - Interaction modes (`idle`, `drawing`, `selecting`, `moving`, `resizing`) prevent conflicting mouse behaviors
---

## [2026-04-12] - story-4
- Implemented shape styling with properties panel
- Features: stroke color (9 presets + custom), fill color (9 presets + custom), stroke width (1/2/4px), stroke style (solid/dashed), fill style (none/solid/hatch)
- Files changed:
  - packages/frontend/src/types/shapes.ts: added ShapeStyle interface, StrokeWidth/StrokeStyle/FillStyle types, DEFAULT_STYLE constant, style field on all shape types
  - packages/frontend/src/components/PropertiesPanel.tsx: new right-side panel showing style controls when shape is selected
  - packages/frontend/src/components/CanvasComponent.tsx: updated drawShape to use shape.style properties, shape creation uses current defaultStyle
  - packages/frontend/src/App.tsx: lifted selectedId state, integrated PropertiesPanel, handleStyleChange updates both selected shape and default style
- TypeScript strict mode and ESLint pass
- **Learnings for future iterations:**
  - rough.js uses `strokeLineDash` (e.g., `[8, 6]`) for dashed strokes, NOT `strokeStyle` or `strokeDashArray`
  - rough.js `fillStyle` uses 'hachure' for hatch patterns, not 'hatch'; 'cross-hatch' also available
  - Style state flows: PropertiesPanel -> App handleStyleChange -> { updates defaultStyle for new shapes + updates selected shape in shapes array }
---

## [2026-04-12] - story-5
- Implemented text tool with inline editing
- Features: Text tool in toolbar, click canvas to create text, Enter or blur to finish editing, double-click to re-edit, text selectable/movable/resizable like other shapes
- Files changed:
  - packages/frontend/src/types/shapes.ts: added TextShape interface, 'text' to ToolType, 'text' to Shape union
  - packages/frontend/src/components/Toolbar.tsx: added Text tool button
  - packages/frontend/src/components/CanvasComponent.tsx: added text rendering via canvas fillText, text placement on click, inline textarea overlay with focus/blur handling, finishEditing/cancelEditing functions, double-click to re-edit existing text
- TypeScript strict mode and ESLint pass
- **Learnings for future iterations:**
  - Text shapes store bounds (width/height) that are computed dynamically from canvas.measureText(); newly created text shapes have width:0/height:0 and bounds are computed on-the-fly in getShapeBounds
  - Text editing overlay uses position:fixed with canvas coordinates; need to guard handleMouseDown when editingText is active to prevent shape creation underneath the overlay
  - Empty text shapes are removed on cancel or blur with empty content
---

## [2026-04-12] - story-6
- Implemented room-based collaboration with WebSocket
- Features: room join via WebSocket, share button copies room URL, user count display, automatic room cleanup after 5 minutes of inactivity
- Files changed:
  - packages/backend/src/index.ts: added room management (Map<roomId, Set<WebSocket>>), join_room message handling, user count broadcasting, room cleanup timers
  - packages/backend/eslint.config.js: added typescript-eslint for TypeScript parsing
  - packages/backend/package.json: added @typescript-eslint/parser and typescript-eslint dev dependencies
  - packages/frontend/src/App.tsx: added WebSocket connection, roomId from useParams, user count state, wsStatus state
  - packages/frontend/src/components/RoomHeader.tsx: new component with room ID display, share button (clipboard API with fallback), user count, connection status indicator
- TypeScript strict mode and ESLint pass for both packages
- **Learnings for future iterations:**
  - Backend ESLint flat config needs `typescript-eslint` plugin to parse TypeScript syntax like generic type parameters
  - WebSocket connections are per-client; roomId is tracked on the ws object via closure
  - Room cleanup uses setTimeout that gets cancelled when a new user joins; empty rooms trigger the timer
  - Vite proxies `/ws` to the backend automatically during dev, so frontend connects to same origin
---

## [2026-04-12] - story-7
- Implemented real-time shape sync via WebSocket
- Features: shape create/update/delete broadcast, full state sync on join, reconnect with exponential backoff, shape attribution with colored borders
- Files changed:
  - packages/backend/src/index.ts: added roomShapes state, broadcastToRoom/sendFullState helpers, shape_create/shape_update/shape_delete/request_sync message handling, user identity assignment (color+name)
  - packages/frontend/src/App.tsx: added WebSocket shape sync (diff-based mutation detection), shape ownership tracking (Map<shapeId, userId>), reconnect logic with backoff, remote cursor state, user identity from server
  - packages/frontend/src/components/CanvasComponent.tsx: added colored border rendering on remote shapes, remote cursor rendering (pointer SVG + name label), cursor position broadcasting on mouse move
- TypeScript strict mode and ESLint pass for both packages
- **Learnings for future iterations:**
  - Shape sync uses diff-based detection (compare length for create/delete, reference comparison for updates) to minimize WebSocket traffic
  - `isRemoteUpdate` ref flag prevents echo loops when receiving shape mutations from the server
  - Reconnect uses exponential backoff (500ms base, 5000ms max) to avoid hammering the server
  - Shape ownership tracked client-side via Map<shapeId, userId>; synced shapes on reconnect are marked as '__remote__' since server doesn't persist ownership
  - userId is `${color}-${name}` format from server-assigned identity; colored borders extract the color by splitting on '-'
---

## [2026-04-12] - story-8
- Implemented live cursor presence with colored pointers and name labels
- Features: remote cursors with pointer SVG + name label (color + animal, e.g. "Purple Fox"), pencil indicator when drawing, cursor position throttled at ~30fps, cursor_leave on disconnect
- Files changed:
  - packages/backend/src/index.ts: cursor_position relay with isDrawing field, cursor_leave broadcast on disconnect
  - packages/frontend/src/App.tsx: RemoteCursor interface with isDrawing, broadcastCursor includes isDrawing state
  - packages/frontend/src/components/CanvasComponent.tsx: hex-to-color-name mapping, pencil indicator SVG overlay on remote cursors, broadcasts isDrawing during draw mode
- TypeScript strict mode and ESLint pass for both packages
- **Learnings for future iterations:**
  - TypeScript flow analysis narrows union types after early returns; `interactionMode === 'drawing'` after a drawing early-return is flagged as always-false. Broadcast cursor position inside the drawing block instead
  - Cursor broadcasting must happen within the drawing mode block (before early return) to capture the `isDrawing: true` state
---

## [2026-04-12] - story-9
- Implemented canvas pan and zoom with canvas context setTransform
- Features: mouse wheel zoom centered on cursor (10%-500%), middle-click drag pan, space+drag pan, zoom level indicator in bottom-right corner, double-click on empty canvas resets zoom to 100%, pan/zoom state is per-user (stored in App.tsx state, not shared)
- Files changed:
  - packages/frontend/src/App.tsx: panX/panY/scale state with setters passed to CanvasComponent
  - packages/frontend/src/components/CanvasComponent.tsx: screenToWorld/worldToScreen coordinate conversion, ctx.setTransform for pan/zoom rendering, native wheel event listener (passive: false), space key tracking, panning interaction mode, zoom indicator overlay, double-click reset, handle/lineWidth scaled by 1/scale for consistent visual size, remote cursor and text overlay positioned with screen coordinates
- TypeScript strict mode and ESLint pass for both packages
- **Learnings for future iterations:**
  - ctx.setTransform is the correct approach for canvas pan/zoom (not CSS transform); rough.js draws in canvas coordinate space so the transform must be applied to the 2D context before drawing
  - Native wheel event listener with { passive: false } is required because React synthetic events cannot prevent the browser's default zoom behavior
  - Overlay elements (text editor, remote cursors) use position:fixed with screen coordinates, must convert world coordinates via (worldX * scale + panX) for correct positioning
  - Handle sizes, line widths, and dash patterns must be divided by scale to appear at consistent visual sizes across zoom levels
---

## [2026-04-12] - v2-story-1
- Implemented Excalidraw-style layout redesign
- Files changed:
  - packages/frontend/src/components/Toolbar.tsx: rewritten as top-centered floating toolbar with minimize toggle, semi-transparent background, rounded corners, shadow, shortcut labels on buttons
  - packages/frontend/src/components/BottomPanel.tsx: new bottom-right floating panel with room ID, share button, user count, connection status, and zoom level
  - packages/frontend/src/App.tsx: removed RoomHeader and PropertiesPanel imports, added BottomPanel with scale prop
  - packages/frontend/src/components/CanvasComponent.tsx: removed duplicate zoom indicator (now in BottomPanel)
  - prd.json: set v2-story-1 passes to true
- TypeScript strict mode and ESLint pass for both packages
- **Learnings for future iterations:**
  - Floating toolbar uses `onMouseDown={(e) => e.stopPropagation()}` to prevent canvas interactions when clicking the toolbar
  - `setDefaultStyle` and `handleStyleChange` commented out in App.tsx (will be re-enabled for v2-story-5)
  - Canvas component canvas spans 100vw/100vh — floating UI elements use position:fixed on top
---

## [2026-04-12] - v2-story-2
- Implemented yellow color theme throughout the application
- Files changed:
  - packages/frontend/src/theme.ts: new theme constants file with yellow color palette (canvasBg: #FFF8E7, panelBg: rgba(255,253,245,0.92), btnActiveBg: #FFF3C4, textPrimary: #333, etc.)
  - packages/frontend/src/components/Toolbar.tsx: applied yellow theme to toolbar background, buttons, active/hover states
  - packages/frontend/src/components/BottomPanel.tsx: applied yellow theme to panel background, buttons, dividers, text colors
  - packages/frontend/src/components/CanvasComponent.tsx: canvas background changed to light yellow, text editor overlay uses yellow-themed border and background
- TypeScript strict mode and ESLint pass for both packages
- **Learnings for future iterations:**
  - Project uses inline styles, not TailwindCSS — theme colors are managed via a centralized `theme.ts` module
  - Default stroke color remains black (#000000) which provides good contrast on the yellow background
  - Remote cursor colors (from server-assigned hex colors) remain visible on the yellow canvas
---

## [2026-04-12] - v2-story-3
- Implemented complete button functions with keyboard shortcuts
- Features added:
  - Line tool (L/4): click-and-drag to draw straight lines with preview, auto-select after creation
  - Eraser tool (X/7): click to delete single shape, drag to erase multiple shapes
  - Ctrl+Y redo with forward history tracking
  - Ctrl+A select all (multi-selection support with bounding boxes for all selected shapes)
  - Clear Canvas button with confirmation dialog
  - Tool keyboard shortcuts: V/1 (Select), R/2 (Rectangle), O/3 (Ellipse), L/4 (Line), P/5 (Freehand), X/7 (Eraser)
  - Auto-select shapes after creation
  - Multi-shape movement when multiple shapes selected
- Files changed:
  - packages/frontend/src/types/shapes.ts: added LineShape interface, 'line' and 'eraser' to ToolType, 'line' to Shape union
  - packages/frontend/src/components/Toolbar.tsx: added Line, Eraser tools and Clear Canvas button with divider
  - packages/frontend/src/App.tsx: added forwardHistory state for redo, multi-selection (selectedIds array), keyboard shortcuts for tool switching/Ctrl+Y/Ctrl+A, clearCanvas function with confirmation
  - packages/frontend/src/components/CanvasComponent.tsx: added Line tool drawing with preview, Line shape rendering and bounds, Eraser click/drag deletion, multi-selection bounding box rendering, multi-shape movement, auto-select on creation
- TypeScript strict mode and ESLint pass for both packages
- **Learnings for future iterations:**
  - `onSelectedIdsChange` expects `string[]` directly, not functional updaters — use `selectedIds.filter(...)` instead of `(prev) => prev.filter(...)`
  - Multi-selection requires iterating over all selected IDs for drawing bboxes (not a single `selectedId`)
  - Forward history is stored separately in App.tsx and cleared when new shapes are created via pushHistory
  - Line shapes use `startX/startY/endX/endY` coordinates (not bounding boxes) but `getShapeBounds` converts to bbox for hit testing and resize
---

## [2026-04-12] - v2-story-4
- Implemented double-click text editing
- Features: removed Text tool button from toolbar, double-click on empty canvas opens inline text editor, double-click existing text shape enters edit mode, Enter confirms text, Escape cancels editing, empty text does not create shape
- Files changed:
  - packages/frontend/src/types/shapes.ts: removed 'text' from ToolType union
  - packages/frontend/src/components/Toolbar.tsx: removed Text tool from TOOLS array
  - packages/frontend/src/components/CanvasComponent.tsx: changed handleDoubleClick to create text on empty canvas (instead of reset zoom), removed activeTool === 'text' mouse handling, removed 'text' cursor style
- TypeScript strict mode and ESLint pass for both packages
- **Learnings for future iterations:**
  - Text creation now triggered via double-click (not via a tool mode) — the TextShape type and editing overlay logic remain unchanged
  - Double-click zoom reset was removed; zoom reset is no longer available via double-click (was never an acceptance criteria in v2)
  - The text editing overlay (textarea with position:fixed) works the same way regardless of how editing is initiated
---

## [2026-04-12] - v2-story-5
- Implemented properties panel integration below toolbar
- Features: floating panel below toolbar with stroke color (8 presets + custom), fill color (8 presets + none + custom), stroke width (3 visual options), stroke style (solid/dashed), fill style (none/solid/hatch)
- When no shape selected, controls set defaults for new shapes
- When shape selected, modifying properties immediately updates that shape
- Files changed:
  - packages/frontend/src/components/PropertiesPanel.tsx: rewritten as top-centered floating panel below toolbar with yellow theme, horizontal layout with labeled sections and visual icon representations
  - packages/frontend/src/App.tsx: imported PropertiesPanel, uncommented handleStyleChange, added panelStyle computed prop that shows selected shape style or default style
- TypeScript strict mode and ESLint pass for both packages
- **Learnings for future iterations:**
  - PropertiesPanel uses `defaultStyle` state in App.tsx for new shape defaults; CanvasComponent receives `defaultStyle` prop directly
  - `panelStyle` computed inline in render picks selected shape's style when any shape is selected, falls back to `defaultStyle`
  - PropertiesPanel positioned at `top: 62px` (below toolbar at `top: 12px` + toolbar height ~50px)
  - Color picker inputs use `position: absolute; opacity: 0` to hide native input while keeping label clickable
---

## [2026-04-12] - v2-redesign-1
- Fixed canvas jitter during drawing and input
- Changes:
  - Added `moveShapesRef` to store intermediate shapes during move/resize operations — avoids React state mutations on every mouseMove
  - `redrawCanvas` uses `moveShapesRef.current ?? shapes` to render intermediate state without triggering re-renders
  - Panning handler updates `panXRef`/`panYRef` and redraws using `applyCanvasTransformFromRefs` — no `onPanXChange`/`onPanYChange` during mouseMove
  - Pan/panY synced to parent state only on mouseUp for consistency with other components
  - Shapes state only committed on mouseUp via `pushHistory`, triggering WebSocket broadcast once per interaction
- TypeScript strict mode and ESLint pass for both packages
- **Learnings for future iterations:**
  - During drag interactions (move/resize/pan), use refs for intermediate state and direct canvas redraw — only commit to React state on mouseUp
  - `applyCanvasTransformFromRefs` uses ref values for pan/scale to avoid dependency on React state during panning
  - `moveShapesRef` is cleared to null on mouseUp, falling back to committed `shapes` state
---

## [2026-04-12] - v2-redesign-2
- Fixed canvas jitter during panning (wheel zoom)
- Changes:
  - Wheel handler now updates `scaleRef`/`panXRef`/`panYRef` directly and redraws canvas immediately using `applyCanvasTransformFromRefs` — no React state updates during scroll
  - Added `zoomCommitTimer` to debounce state sync (150ms after scroll pauses)
  - State is committed via `commitZoomToState` callback only after zoom interaction settles
  - BottomPanel zoom indicator updates after debounce delay; canvas itself zooms smoothly in real-time
- TypeScript strict mode and ESLint pass for both packages
- **Learnings for future iterations:**
  - Both wheel zoom and drag panning must use the same ref-based pattern to avoid jitter — update refs, redraw immediately, defer state sync
  - `commitZoomToState` is a `useCallback` that depends on `onScaleChange`/`onPanXChange`/`onPanYChange` — these are stable props from App.tsx
  - The wheel handler's useEffect depends on `shapes`, `selectedIds`, `getBBox`, `getHandlePositions`, `userId`, `shapeOwners`, `applyCanvasTransformFromRefs`, `commitZoomToState` to properly redraw all canvas elements during zoom
---

## [2026-04-12] - v2-redesign-3
- Implemented rhombus/diamond shape tool
- Features: rhombus button in toolbar (icon: ◇, shortcut: D), click-and-drag to draw diamond shapes, supports select/move/resize, solid and outline fill modes work via rc.polygon
- Files changed:
  - packages/frontend/src/types/shapes.ts: added RhombusShape interface (x/y/width/height), added 'rhombus' to ToolType and Shape union
  - packages/frontend/src/components/Toolbar.tsx: added rhombus tool button with shortcut D
  - packages/frontend/src/components/CanvasComponent.tsx: added rhombus drawing via rc.polygon connecting 4 vertices (top, right, bottom, left), rhombus preview during drag, rhombus shape creation on mouseUp
  - packages/frontend/src/App.tsx: added 'd' keyboard shortcut for rhombus tool
- TypeScript strict mode and ESLint pass for both packages
- **Learnings for future iterations:**
  - Rhombus uses bounding box (x/y/width/height) like rectangle/ellipse — existing hit detection, move, and resize logic work automatically without modification
  - rc.polygon takes an array of [x, y] coordinate pairs: 4 vertices for rhombus (top center, right center, bottom center, left center)
  - Preview drawing during drag uses the same rc.polygon call with identical vertex calculations
---

## [2026-04-12] - v2-redesign-4
- Implemented arrow shape tool
- Features: arrow button in toolbar (icon: →, shortcut: A), click-and-drag to draw arrows with arrowhead, supports solid/dashed line styles, select/move/resize like line shapes
- Files changed:
  - packages/frontend/src/types/shapes.ts: added ArrowShape interface (startX/startY/endX/endY), added 'arrow' to ToolType and Shape union
  - packages/frontend/src/components/Toolbar.tsx: added arrow tool button with shortcut A
  - packages/frontend/src/components/CanvasComponent.tsx: added arrow rendering via rc.linearPath + canvas arrowhead triangle (strokeWidth * 3), arrow preview during drag, arrow shape creation on mouseUp, getShapeBounds/applyResize/move logic handle arrow type
  - packages/frontend/src/App.tsx: added 'a' keyboard shortcut for arrow tool
- TypeScript strict mode and ESLint pass for both packages
- **Learnings for future iterations:**
  - ArrowShape uses the same startX/startY/endX/endY coordinate pattern as LineShape — getShapeBounds, applyResize, and move logic are shared between the two types
  - Arrowhead rendering uses native canvas (ctx) rather than roughjs — draw a filled triangle rotated to the line angle via ctx.translate + ctx.rotate
  - Arrowhead size is proportional to strokeWidth * 3 for visual consistency across stroke widths
---

## [2026-04-12] - v2-redesign-5
- Implemented image insert feature
- Features: image button in toolbar (icon: 🖼, shortcut: I), file picker opens on click/keypress, selected image converted to Data URL, image added as shape with default width 200px maintaining aspect ratio, image supports select/move/resize like rectangle/ellipse
- Files changed:
  - packages/frontend/src/types/shapes.ts: added ImageShape interface (x/y/width/height/src), added 'image' to Shape union
  - packages/frontend/src/components/Toolbar.tsx: added image button with file picker, removed minimize toggle, added onImageInsert callback prop
  - packages/frontend/src/components/CanvasComponent.tsx: added image rendering via ctx.drawImage with image cache ref to avoid reloading, added imageCacheRef for performance
  - packages/frontend/src/App.tsx: added handleImageInsert function, 'i' keyboard shortcut triggers file picker, image positioned at visible canvas coordinates
- TypeScript strict mode and ESLint pass for both packages
- **Learnings for future iterations:**
  - ImageShape uses x/y/width/height like RectangleShape — existing getShapeBounds, applyResize, and move logic work automatically without modification
  - Images should be cached (Map<src, HTMLImageElement>) to avoid reloading on every canvas redraw
  - ctx.drawImage only works when img.complete && img.naturalWidth > 0 (image fully loaded)
  - Image insertion positions shapes at visible viewport coordinates (-panX/scale + offset) so images appear on screen
---

## [2026-04-12] - v2-redesign-6
- Implemented separate solid and outline shape buttons
- Changes:
  - Toolbar: split rectangle/ellipse/rhombus into outline (▭/○/◇) and solid (▮/●/◆) button pairs
  - Outline buttons set `fillStyle: 'none'` via `onFillStyleChange` callback
  - Solid buttons set `fillStyle: 'solid'` via `onFillStyleChange` callback
  - Removed Fill Color selector section from PropertiesPanel
  - Removed Fill Style (none/solid/hatch) selector section from PropertiesPanel
  - Added `-solid` variants to ToolType union type
  - CanvasComponent uses `getBaseShapeTool()` helper to map solid tool variants to base shape type for drawing/hit-testing logic
  - Shape creation uses `defaultStyle.fillStyle` which is set by toolbar button clicks
- Files changed:
  - packages/frontend/src/types/shapes.ts: extended ToolType with 'rectangle-solid', 'ellipse-solid', 'rhombus-solid'
  - packages/frontend/src/components/Toolbar.tsx: added outline/solid button pairs with fillStyle prop, onFillStyleChange callback
  - packages/frontend/src/components/PropertiesPanel.tsx: removed Fill Color and Fill Style sections, removed FILL_PRESETS
  - packages/frontend/src/components/CanvasComponent.tsx: added getBaseShapeTool() helper, updated shape creation/preview checks
  - packages/frontend/src/App.tsx: added handleFillStyleChange, passed to Toolbar
- TypeScript strict mode and ESLint pass for both packages
- **Learnings for future iterations:**
  - Shape tool variants (`-solid`) map to base types for canvas drawing logic — the fillStyle difference is handled through `defaultStyle` rather than branching on tool type in drawShape
  - Removing UI sections from PropertiesPanel (Fill Color, Fill Style) simplifies the panel by ~140 lines of code
  - `getBaseShapeTool()` helper is a clean way to extend ToolType without duplicating drawing/hit-testing logic for each variant
---

## [2026-04-12] - v2-redesign-7
- Implemented unified color picker bar
- Changes:
  - Toolbar: added second row with 10 color buttons + custom color picker (+ button)
  - Colors: black, red, blue, green, purple, orange, brown, gray, pink, cyan
  - Selected color applies to both strokeColor and fillColor simultaneously
  - Added `unifiedColor` state in App.tsx, synced when selecting existing shapes
  - PropertiesPanel: removed Stroke Color section (kept Width and Style)
  - PropertiesPanel repositioned at top: 120px (below two-row toolbar)
- Files changed:
  - packages/frontend/src/App.tsx: added unifiedColor state, handleColorChange function, useEffect to sync color on shape selection
  - packages/frontend/src/components/Toolbar.tsx: two-row layout, UNIFIED_COLORS constant, circular color swatches with active border, custom color picker
  - packages/frontend/src/components/PropertiesPanel.tsx: removed stroke color selector section, repositioned below taller toolbar
- TypeScript strict mode and ESLint pass for both packages
- **Learnings for future iterations:**
  - Unified color means a single `unifiedColor` state drives both strokeColor and fillColor — no separate color management needed
  - When selecting existing shapes, unifiedColor should sync to the shape's strokeColor so the picker reflects current selection
  - Color swatches use circular borders (borderRadius: '50%') to distinguish from the square ones in PropertiesPanel

---

## [2026-04-12] - v2-redesign-8
- Implemented pixel-based eraser using globalCompositeOperation = 'destination-out'
- Eraser no longer deletes entire shapes — erases only pixels within eraser stroke path
- Eraser has adjustable radius (default 20px, adjustable via width/size control in PropertiesPanel)
- Eraser path is smooth with interpolation between consecutive points
- Erase effect visible in real time during mouse drag
- Eraser cursor indicator shows circle with current radius
- Shapes data is not modified by pixel erase (MVP scope)
- Files changed:
  - packages/frontend/src/components/CanvasComponent.tsx: added eraserPoints/lastEraserPoint/mousePos refs, replaced shape deletion with pixel erase using destination-out compositing, added eraser cursor indicator overlay
  - packages/frontend/src/components/PropertiesPanel.tsx: added activeTool/eraserRadius/onEraserRadiusChange props, shows "Size" label with circle icons when eraser is active, maps strokeWidth values (1,2,4) to eraser radii (10px, 20px, 40px)
  - packages/frontend/src/App.tsx: added eraserRadius state, passed to PropertiesPanel and CanvasComponent
- TypeScript strict mode and ESLint pass for both packages
- **Learnings for future iterations:**
  - `globalCompositeOperation = 'destination-out'` erases pixels by making them transparent — works after shapes are drawn, so erased areas show the canvas background
  - Eraser rendering uses both circle fills at each point AND a stroked line connecting all points for smooth continuous erase without gaps
  - Interpolation step size is `eraserRadius / 3` — ensures no gaps between erase circles even at high mouse speeds
  - `redrawCanvas` useCallback now depends on `eraserRadius` — changing eraser size triggers full redraw to update both erased pixels and cursor indicator
  - PropertiesPanel conditionally adapts its width control to show eraser size when eraser tool is active

---

## [2026-04-12] - v2-redesign-9
- Implemented unified two-row toolbar merging Toolbar + PropertiesPanel
- Changes:
  - Deleted Toolbar.tsx and PropertiesPanel.tsx components
  - Created UnifiedToolbar.tsx with two rows: Row 1 (tools) + Row 2 (colors — width — style)
  - Row 2 layout: 10 color buttons + custom picker | divider | width selector (3 options) | divider | line style (solid/dashed)
  - handleStyleChange in App.tsx simplified to accept partial patches ({ strokeWidth?, strokeStyle? })
  - App.tsx imports updated, JSX replaced single Toolbar + PropertiesPanel with UnifiedToolbar
- TypeScript strict mode and ESLint pass for both packages
- **Learnings for future iterations:**
  - UnifiedToolbar combines all UI controls into one component — no need to coordinate positioning between separate floating panels
  - Row 2 uses a single flex container with dividers as visual separators between color/width/style sections
  - Width and style sections use smaller font sizes (9px labels) and tighter gaps to fit within the toolbar footprint

---

## [2026-04-12] - v2-redesign-10
- Implemented draggable and lockable toolbar
- Changes:
  - UnifiedToolbar: added drag handle (⠿) as first button, click and drag to reposition toolbar
  - UnifiedToolbar: added lock toggle button (🔓/🔒) as second button
  - UnifiedToolbar: toolbar position tracked via `toolbarPos` state (x/y offsets from center)
  - UnifiedToolbar: drag handle visible on hover or during drag, grab/grabbing cursor
  - App.tsx: added `locked` state, passed to UnifiedToolbar and CanvasComponent
  - CanvasComponent: locked state blocks handleMouseDown for all tools except panning (middle-click/space+drag)
  - CanvasComponent: locked state blocks handleDoubleClick text editing
  - CanvasComponent: locked state blocks Delete/Backspace shape deletion and Ctrl+Z undo
  - CanvasComponent: cursor shows 'not-allowed' when locked
  - Pan and zoom still work when canvas is locked
- TypeScript strict mode and ESLint pass for both packages
- **Learnings for future iterations:**
  - Toolbar drag uses `calc(50% + ${toolbarPos.x}px)` for horizontal centering + offset pattern
  - Lock is a simple boolean gate in handleMouseDown that only allows panning mode to be set
  - Keyboard handlers (Delete, Ctrl+Z) also need lock checks since they bypass mouse handlers

## [2026-04-13] - v2.1-bugfix-1
- Verified shapes persistence after draw completion - all shape types (rectangle, ellipse, rhombus, line, arrow, freehand, image, text) correctly persist after mouseUp
- Confirmed handleMouseUp creates newShape and pushes it into shapes array via pushHistory([...shapes, newShape])
- Confirmed redrawCanvas is triggered after shapes state update (via useEffect on shapes dependency)
- No code changes needed - implementation was already correct
- TypeScript strict mode and ESLint pass
- **Learnings for future iterations:**
  - Shape persistence relies on pushHistory which calls onShapesChange with [...shapes, newShape] — the diff-based mutation detection in onShapesChange correctly identifies the new shape and broadcasts via WebSocket
  - moveShapesRef is used for intermediate move/resize state; falls back to committed shapes state via `moveShapesRef.current ?? shapes` in redrawCanvas
  - Image and text shapes use pushHistory from handleDoubleClick (text) and keyboard shortcut (image) rather than mouseUp

## [2026-04-13] - v2.1-bugfix-2
- Verified all tool buttons correctly activate their drawing modes
- Confirmed all tool button onClick handlers call onToolChange (UnifiedToolbar line 234)
- Confirmed CanvasComponent has branches for all tool types in handleMouseDown/handleMouseMove/handleMouseUp
- Confirmed keyboard shortcuts: V/1=Select, R/2=Rectangle, O/3=Ellipse, D/Rhombus, L/4=Line, A=Arrow, P/5=Freehand, I=Image, X/7=Eraser
- Eraser handled separately in handleMouseDown (line 893-901) and handleMouseUp (line 1231-1234)
- No code changes needed - implementation was already correct
- TypeScript strict mode and ESLint pass
- **Learnings for future iterations:**
  - Image tool is not a ToolType — it's handled via a separate keyboard shortcut (line 330-367) that opens a file picker
  - Solid tool variants (rectangle-solid, ellipse-solid, rhombus-solid) use getBaseShapeTool() helper to map to base types for canvas drawing logic
  - Eraser bypasses the standard drawing flow — it tracks points for pixel-level erasing via destination-out compositing, not shape creation
---

## [2026-04-13] - v2.1-save
- Implemented PNG export of canvas content
- Features: save button in UnifiedToolbar Row 2 (download icon + "Save" label), exports all shapes' minimum bounding box + 20px padding as .png, browser native download via canvas.toBlob() + <a download>, filename format: whiteboard-{roomId}-{timestamp}.png
- Files changed:
  - packages/frontend/src/components/CanvasComponent.tsx: added forwardRef + useImperativeHandle to expose exportPng function, moved getShapeBounds to module level for stable reference, export renders all shape types (rectangle, ellipse, rhombus, line, arrow, freehand, image, text) to offscreen canvas with rough.js, background matches theme.canvasBg
  - packages/frontend/src/components/UnifiedToolbar.tsx: added onSave prop, added save button in Row 2 with SVG download icon
  - packages/frontend/src/App.tsx: added canvasRef for CanvasComponent, added handleExportPng callback, passed onSave to UnifiedToolbar and ref to CanvasComponent
- TypeScript strict mode and ESLint pass
- **Learnings for future iterations:**
  - forwardRef requires function signature `(props, ref)` — ref is the second parameter
  - useImperativeHandle is the cleanest way to expose child component methods to parent without prop drilling callbacks
  - rough.js uses `strokeLineDash` (array) for dashed strokes, NOT `strokeStyle`
  - Module-level helper functions (like getShapeBounds) are stable references and don't cause re-render issues when used in hook dependency arrays
  - Offscreen canvas export works by creating a new canvas element, rendering shapes with rough.js, then using canvas.toBlob() for download

---

## [2026-04-13] - v2.1-clear
- Implemented clear canvas with stable undo support
- Features: moved trash icon button to UnifiedToolbar Row 2 (SVG trash icon), removed confirmation dialog, clears all shapes on click, pushes complete current state snapshot to history before clearing, Ctrl+Z restores pre-clear state, selectedIds reset to empty after clear, WebSocket broadcasts all shape deletions to other users
- Files changed:
  - packages/frontend/src/components/UnifiedToolbar.tsx: moved trash button from Row 1 to Row 2 (after Save button), replaced emoji with SVG trash icon, styled to match Row 2 compact button style
  - packages/frontend/src/App.tsx: removed window.confirm() from clearCanvas function — now clears immediately without confirmation dialog
- TypeScript strict mode and ESLint pass
- **Learnings for future iterations:**
  - clearCanvas captures `shapes` via useCallback dependency — this snapshot is pushed to history, ensuring undo restores the exact pre-clear state
  - Row 1 and Row 2 `<div>` containers in UnifiedToolbar must both be properly closed — removing a button from Row 1 also removed the closing `</div>` tag
  - Forward history is cleared on clear (setForwardHistory([])) so redo isn't available after clearing — this is intentional since clearing starts a new action branch

## [2026-04-13] - v2.1-theme
- Implemented light/dark theme toggle
- Features: sun/moon toggle buttons in UnifiedToolbar Row 2, light mode canvas background #FFFFFF, dark mode canvas background #121212, toolbar/panel/button/text colors adapt to theme, stroke colors inverted via render-time color mapping in dark mode (#000000 → #E0E0E0), PNG export background and stroke colors match current theme, theme state in App.tsx (not persisted, defaults to light)
- Files changed:
  - packages/frontend/src/theme.ts: replaced single `theme` export with `getThemeColors(mode)`, `getStrokeColor(color, mode)` functions and `ThemeMode` type. Added darkTheme palette with #121212 canvasBg and DARK_MODE_COLOR_MAP for stroke color inversion
  - packages/frontend/src/App.tsx: added `themeMode` state (defaults to 'light'), passed to UnifiedToolbar, CanvasComponent, and BottomPanel
  - packages/frontend/src/components/UnifiedToolbar.tsx: accepts `themeMode` and `onThemeChange` props, computes `theme` from `getThemeColors(themeMode)`, added sun (moon icon to switch to dark) and moon (sun icon to switch to light) toggle buttons in Row 2
  - packages/frontend/src/components/CanvasComponent.tsx: accepts `themeMode` prop, uses `getStrokeColor(color, themeMode)` for all stroke colors in drawShape, preview drawing, and PNG export — ensures #000000 becomes #E0E0E0 in dark mode via render-time mapping
  - packages/frontend/src/components/BottomPanel.tsx: accepts `themeMode` prop, computes `theme` from `getThemeColors(themeMode)`
- TypeScript strict mode and ESLint pass
- **Learnings for future iterations:**
  - Stroke color inversion uses `getStrokeColor(color, themeMode)` — a pure function that maps dark colors to light variants at render time. Does NOT modify shape.style.strokeColor, keeping stored colors intact
  - `theme` is computed via `getThemeColors(themeMode)` at component top level — all `theme.*` references automatically adapt without individual updates
  - ESLint `react-hooks/exhaustive-deps` flags `theme.canvasBg` as missing dependency — since `theme` is derived from `themeMode`, add `themeMode` to deps and use `eslint-disable` comment for the `theme.*` false positives
  - Preview drawing (handleMouseMove) also needs `getStrokeColor` for stroke colors — otherwise shape previews during drag will use light-mode colors in dark mode

## [2026-04-13] - v2.1-zoom
- Implemented zoom in/out buttons in bottom-right panel
- Features: capsule-shaped zoom control with [-] shrink → percentage text → [+] enlarge, 25% step per click, range 10%-500%, viewport center zoom, mouse wheel zoom still works alongside buttons
- Files changed:
  - packages/frontend/src/App.tsx: added handleZoomIn/handleZoomOut callbacks using the same scale-ratio pan adjustment as wheel handler but centered on viewport center
  - packages/frontend/src/components/BottomPanel.tsx: replaced plain zoom percentage with capsule-shaped zoom control group containing minus button, percentage label, plus button
  - packages/frontend/src/theme.ts: added zoomBg color to light and dark themes
- TypeScript strict mode and ESLint pass
- **Learnings for future iterations:**
  - Zoom centered on viewport uses window.innerWidth/2 and window.innerHeight/2 as center point — same scale-ratio formula as mouse-centered wheel zoom
  - Capsule zoom control uses borderRadius: '12px' on outer container with transparent button backgrounds that show hover via backgroundColor transition
  - Zoom buttons use Unicode minus (−) instead of hyphen for visual consistency with plus sign

## [2026-04-13] - v2.1-history
- Implemented undo/redo buttons in bottom-right panel
- Features: undo (left curved arrow) and redo (right curved arrow) buttons in capsule next to zoom controls, undo reverts to previous canvas state (disabled when history empty), redo restores after undo (cleared on new action), uses existing history/forwardHistory stacks, selectedIds cleared on undo/redo, matches existing Ctrl+Z/Ctrl+Y keyboard shortcuts
- Files changed:
  - packages/frontend/src/components/CanvasComponent.tsx: added forwardHistory/onForwardHistoryChange props, added redo useCallback function, exposed undo/redo via useImperativeHandle in CanvasComponentRef
  - packages/frontend/src/App.tsx: added handleUndo/handleRedo callbacks via canvasRef, passed forwardHistory/onForwardHistoryChange to CanvasComponent, passed onUndo/onRedo/canUndo/canRedo to BottomPanel
  - packages/frontend/src/components/BottomPanel.tsx: added onUndo/onRedo/canUndo/canRedo props, added undo/redo button capsule with SVG curved arrow icons before zoom controls, disabled state styling
- TypeScript strict mode and ESLint pass
- **Learnings for future iterations:**
  - Redo function mirrors the Ctrl+Y handler in App.tsx — both push current shapes to history and pop from forwardHistory
  - useImperativeHandle is used to expose undo/redo to parent so BottomPanel can trigger them without prop drilling through CanvasComponent
  - Undo/redo buttons use the same capsule container style (zoomBg, borderRadius: '12px') as the zoom control for visual consistency
  - Button disabled state uses theme.textMuted color and cursor: 'default' to clearly indicate non-interactivity
