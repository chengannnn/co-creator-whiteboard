# Ralph Progress Log
Started: Tue Apr 15 2026
---

## Codebase Patterns
- Closed shapes (rectangle, ellipse, rhombus) share `createBBoxHandler` factory in RectangleHandler.ts — all use `Math.min`/`Math.abs` for coordinate normalization
- Vector shapes (line, arrow) preserve direction via `points` array with signed deltas; `width`/`height` store absolute values separately
- Freehand normalizes by recomputing bbox from points and shifting all points relative to min values
- Element creation uses `createBaseElement` helper in helpers.ts which initializes `groupIds: []`
- Three-layer canvas: bg (background), static (committed shapes), interactive (draft/in-progress shapes)
- Resize uses absolute coordinate calculation: `applyResize` receives `mouseX`/`mouseY` and recalculates dimensions from anchor corner (CanvasComponent.tsx + SelectToolHandler.ts)
- hitTesting.ts exports `getWorldBounds`, `pointInBoundingBox`, `findElementsAtPoint`, `rectsIntersect`, and `findElementsInRect` for bounding box operations
- Marquee selection uses explicit `renderInteractive()` call from pointer handlers — ref-based state avoids unnecessary React re-renders

## [2026-04-15 17:44] - US-001
- Verified US-001 implementation: reverse drag coordinate normalization already in place
- Coordinate normalization for closed shapes: `createBBoxHandler` uses `Math.min(startX, currentX)` and `Math.abs()` (RectangleHandler.ts:35-38)
- Direction preservation for vector shapes: `createLineHandler` stores signed dx/dy in points array (LineHandler.ts:46-57)
- Freehand normalization: `createFreehandHandler` recomputes bbox and normalizes relative to min (FreehandHandler.ts:42-54)
- No code changes needed — all acceptance criteria met
- **Learnings for future iterations:**
  - The `getBaseShapeTool` function in CanvasComponent maps tool variants (e.g., `rectangle-solid`) to base types for handler logic
  - `MIN_DIMENSION = 3` in RectangleHandler prevents tiny shapes from being committed
  - All shape drawing uses native Canvas 2D APIs (drawShape function at CanvasComponent.tsx:339)
---

## [2026-04-15 18:10] - US-002
- Implemented absolute coordinate calculation for resize handles
- CanvasComponent.tsx: Rewrote `applyResize` to use `mouseX`/`mouseY` directly instead of `dx`/`dy` deltas. Each handle locks its opposite anchor corner and recalculates dimensions absolutely. Minimum dimension changed from 1px to 5px.
- SelectToolHandler.ts: Same rewrite applied to duplicate `applyResize` function. Removed unused `resizeStartBounds` from state.
- Typecheck passes
- **Learnings for future iterations:**
  - There are two copies of resize logic: CanvasComponent.tsx (primary, used by React pointer events) and SelectToolHandler.ts (standalone handler). Both must be kept in sync.
  - The old delta-based approach (`width += dx`) caused drift because deltas were computed from original bounds but applied to already-modified bounds.
---

## [2026-04-15 18:45] - US-003
- Implemented marquee selection for batch-selecting shapes
- hitTesting.ts: Added `rectsIntersect` (AABB intersection) and `findElementsInRect` (find all elements intersecting a rectangle) utilities
- CanvasComponent.tsx: Added `marqueeStart` and `marqueeEnd` refs for tracking drag rectangle
- onPointerDown: When select tool clicks on empty canvas, stores marquee start point and enters 'selecting' mode
- onPointerMove: In 'selecting' mode, updates marquee end point and triggers renderInteractive to draw the rectangle
- onPointerUp: Computes normalized marquee bbox, intersects with all scene elements via findElementsInRect, updates selection
- renderInteractive: Draws semi-transparent blue rectangle (fill rgba(0,120,255,0.1) + blue dashed border)
- Typecheck and lint pass
- **Learnings for future iterations:**
  - CanvasComponent has its own duplicated getElementBounds/getBBox/hitTestElements instead of using hitTesting.ts exports — the new findElementsInRect was imported but the local versions remain
  - The renderInteractive function is called via explicit renderInteractive() from pointer handlers (not via React state), so ref-based marquee tracking works without re-rendering
  - Minimum marquee threshold of 3px prevents accidental clicks from triggering selection
---

## [2026-04-15 19:15] - US-004
- Verified SceneElement interface already has `groupIds: string[]` property in BaseElement (element.ts:39)
- Verified createBaseElement already initializes `groupIds: []` (helpers.ts:44)
- Verified image element creation in App.tsx already includes `groupIds: []` (lines 386, 469)
- Fixed shapeToElement function to preserve `groupIds` from incoming WebSocket shapes instead of always defaulting to `[]`. Added `groupIds?: string[]` to the shape parameter type (App.tsx:610) and changed commonBase to use `shape.groupIds ?? []` (App.tsx:626)
- Backend's Shape interface uses `[key: string]: unknown` so groupIds passes through automatically in shape_create/shape_update broadcasts
- Typecheck passes
- **Learnings for future iterations:**
  - The `shapeToElement` function is the ONLY place where groupIds could be lost during WebSocket sync — always check it when adding new element fields
  - Backend stores shapes as generic `Shape` with index signature, so any field added to SceneElement will pass through without backend changes
  - There are 4 places where elements are created: createBaseElement (tools), shapeToElement (WS sync), and two inline image element creations in App.tsx — all must be kept in sync for new fields
---
